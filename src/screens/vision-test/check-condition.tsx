import React from 'react';
import {Text} from 'react-native';
import {Screen} from '~core/ui';

const CheckConditionScreen = () => {
  return (
    <Screen>
      <Text>CheckConditionScreen</Text>
    </Screen>
  );
};

export default CheckConditionScreen;
